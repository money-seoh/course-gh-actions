name: upper-build-go

on:
  push:
    paths:
      - ".github/workflows/upper-build-go.yml"
      - "upper/**"
  workflow_dispatch: # manual

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - run: go version
